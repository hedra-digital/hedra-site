<% if current_page? root_path %>
  <h1 class="logo hide-text">Hedra</h1>
<% else %>
  <p class="logo hide-text"><%= link_to 'Hedra', root_path %></p>
<% end %>
<nav id="main-nav" role="navigation">
  <ul class="nav nav-pills">
    <li>
      <a href="#">Catálogo <span class="dropdown-arrow">&#x2304;</span></a>
      <ul>
        <% @categories.each do |category| %>
          <%= content_tag :li, link_to(category.name, category_path(category)) %>
        <% end %>
      </ul>
    </li>
    <li><%= link_to 'Sobre', about_path %></li>
    <li><%= link_to 'Contato', contact_path %></li>
  </ul>
  <form class="navbar-search" action="/livros/busca" method="get">
    <input type="text" class="search-query" name="term" placeholder="Busca">
  </form>
</nav>
