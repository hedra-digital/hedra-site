$('#category-list .container').append("<%= j render :partial => 'pages/book', :collection => @books %>");
<% if @books.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @books %>');
  $('.pagination').hide();
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
