<% title @category.name %>
<div class="container">
  <%= yield :h1 %>
</div>
<div id="category-list">
  <div class="container">
    <%= render :partial => 'pages/book', :collection => @books %>
    <%= will_paginate @posts %>
  </div>
</div>
