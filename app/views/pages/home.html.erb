<h2>Indicação nossa</h2>

<% @features.each do |f| %>
  <article class="book-list-item row">
    <div class="span2">
      <img src="/assets/l-a-volta-do-parafuso.jpg">
    </div>
    <div class="span10">
      <h3><%= f.book.title %></h3>
      <h4><%= f.book.people.first.name %></h4>
      <div class="book-description">
        <p><%= f.book.description %></p>
      </div>
    </div>
  </article>
<% end %>
