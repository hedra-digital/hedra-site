Ola, <%= @data[:partner_name].titleize %>.

Este e-mail é enviando automáticamente como relatório de suas vendas em parceria com a Editora Hedra durante os últimos 07 dias.

<% if @data[:orders] > 0 %>
Ao todo, foram <%= @data[:orders] %> pedidos a partir do seu link no período de: <%=@data[:start_date]%> até <%=@data[:end_date]%>.
<% else %>
Durante o período de: <%=@data[:start_date]%> até <%=@data[:end_date]%>, não houveram pedidos a partir do seu link.
<% end %>

<% @data[:items].each do |item| %>
<%= " #{item[:title]} (#{item[:people].join(', ')}) #{item[:quantity]} exemplares R$ #{'%.2f' % item[:price]}" %>
<% end %>

O total desta semana é de R$ <%= '%.2f' % @data[:revenue_week] %>.

A soma total do mês se resume a R$ <%= '%.2f' % @data[:revenue_month] %> e será acertado no último dia do período.

Atenciosamente,

Editora Hedra.