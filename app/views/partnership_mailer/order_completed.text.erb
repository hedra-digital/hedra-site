Olá, <%= @partner.name.titleize %>

Um cliente acaba de comprar em nosso site através de seu link especial hedra.

hedra.com.br/coupon/<%=@promotion.slug%>

Confira abaixo as compras e a sua comissão. 

----------------------------------------------------------------
<% @order.order_items.each do |item| %>
<%= "#{item.book.title} (#{item.book.participations.map{|participation| participation.person.name }.join(', ')}) #{item.quantity} exemplares R$ #{'%.2f' % item.price}" %>
<% end %>

----------------------------------------------------------------
Total: <%= '%.2f' %  @total %> (comissão <%= '%.2f' %  @total_comission %>)

Atenciosamente,

Editora Hedra.